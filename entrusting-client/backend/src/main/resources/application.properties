server.port=${ENTRUSTING_BACKEND_PORT:8085}
spring.application.name=entrusting-backend

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:entrusting_db}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.error.include-message=always
server.error.include-binding-errors=always

# Trustee Provider API Configuration
trustee.api.base-url=${TRUSTEE_API_URL:http://localhost:8088}

# CORS Configuration
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5175}
